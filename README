                                   libmsgsvc

A library for sending messages between systems using chat servers. Currently
this only supports IRC.


CLIENT

The client is like a "netcat" connection. It just sends what ever you
type across the wire and prints out what it sees. The client is mainly an
example and for debugging though.
